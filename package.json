{
  "name": "ctbbp-summaries",
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "cf-preview": "astro build && wrangler pages dev dist",
    "deploy": "astro build && wrangler pages deploy dist"
  },
  "dependencies": {
    "astro": "^5.7.0"
  },
  "devDependencies": {
    "@astrojs/cloudflare": "^12.0.0",
    "wrangler": "^4.0.0"
  }
}
